# Impede que um app use dependência que não declarou (hoisting fantasma).
# É o que mantém a fronteira entre api/store/admin real e não apenas convencionada.
strict-peer-dependencies=false
auto-install-peers=true
