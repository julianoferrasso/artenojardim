node_modules/
dist/
build/
.next/
out/

# Segredos — nunca versionar. .env.example é o contrato, .env é a instância.
.env
.env.*
!.env.example

# Storage local (driver de desenvolvimento). Em produção é R2.
# Ancorado no caminho EXATO: `uploads/` solto bate em qualquer diretório com
# esse nome — inclusive src/modules/uploads, o código do módulo. Já engoliu o
# módulo inteiro uma vez.
/apps/api/uploads/

# Logs
*.log
logs/

coverage/
.turbo/
.DS_Store
Thumbs.db
*.tsbuildinfo
